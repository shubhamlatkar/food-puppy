server:
  port: 8085

spring:
  profiles:
    active: ${PROFILE}
  application:
    name: delivery
  cloud:
    config:
      uri: http://localhost:8888

---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://configuration:8888
