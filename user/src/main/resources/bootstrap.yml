server:
  port: 8081

spring:
  profiles:
    active: ${PROFILE}
  application:
    name: user
  cloud:
    config:
      uri: http://localhost:8888
---
spring:
  cloud:
    config:
      uri: http://configuration:8888