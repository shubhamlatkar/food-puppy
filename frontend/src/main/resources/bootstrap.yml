server:
  port: 8090

spring:
  profiles:
    active: ${PROFILE}
  application:
    name: frontend
  cloud:
    config:
      uri: http://localhost:8888

---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://configuration:8888
