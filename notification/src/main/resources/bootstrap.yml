server:
  port: 8083

spring:
  profiles:
    active: ${PROFILE}
  application:
    name: notification
  data:
    mongodb:
      uri: mongodb://2.tcp.ngrok.io:17507/foodrid-notification
      repositories:
        enabled: true
  activemq:
    broker-url: tcp://0.tcp.ngrok.io:13394
    user: admin
    password: admin
    packages:
      trust-all: true